# env

A cross platform `env` library for [lde](https://lde.sh).

It allows you to do things like get and set environment variables, get the current executable path, etc.

## Usage

```
lde add env --git https://github.com/lde-org/env
```
